Multithreading-epoll是一个基于多线程的TCP/UDP的服务器，功能如下：
1.基于epoll或iocp，监听指定端口的TCP请求；
2.TCP长连接、短连接分别使用不同的端口；
3.当接收到客户端的TCP短连接请求时，返回服务器ip地址、MAC地址；当接收到客户端的TCP长连接请求时，服务器接收连接，客户端周期性向服务器发送心跳；
4.支持多个客户端的并发TCP请求；
5.使用多线程技术，同时监听、提供服务；
6.设计服务器端和客户端的通信协议，使用JSON格式。
